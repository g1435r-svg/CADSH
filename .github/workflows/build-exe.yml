name: Build Windows EXE
<<<<<<< HEAD
# Builds a portable offline EXE for Windows (chart.js + xlsx bundled locally)
# Updated: 2026-07-26b
=======
>>>>>>> origin/main

on:
  push:
    branches:
      - main
      - master
<<<<<<< HEAD
      - copilot/**
  workflow_dispatch:

jobs:
  build-windows-exe:
    runs-on: windows-latest
    permissions:
      contents: write
=======
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows-exe:
    runs-on: windows-latest
>>>>>>> origin/main
    defaults:
      run:
        working-directory: maaser-chomesh-upload

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build portable EXE
        run: npm run build:exe

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: maaser-chomesh-windows-exe
          path: maaser-chomesh-upload/dist/*.exe
          if-no-files-found: error

<<<<<<< HEAD
      - name: Create GitHub Release
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: MaaserChomesh - Build ${{ github.run_number }}
          body: |
            ### מחשבון מעשרות וחומש
            קובץ EXE נייד להורדה והפעלה ישירה ב-Windows, ללא התקנה.

            **הורדה:** לחץ על הקובץ `MaaserChomesh*.exe` מטה.
          files: maaser-chomesh-upload/dist/*.exe
          make_latest: true
=======
      - name: Publish GitHub Release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest-build --yes 2>/dev/null || true
          gh release create latest-build dist/MaaserChomesh.exe \
            --repo "$GITHUB_REPOSITORY" \
            --title "MaaserChomesh - Latest Build" \
            --notes "## הורדה ישירה

לחץ על **MaaserChomesh.exe** למטה להורדה והרצה מיידית על Windows.

> הקובץ נבנה אוטומטית ב-GitHub Actions." \
            --latest
>>>>>>> origin/main
